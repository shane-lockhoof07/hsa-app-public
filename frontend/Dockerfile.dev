FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Add --host flag to bind to 0.0.0.0 instead of localhost
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]